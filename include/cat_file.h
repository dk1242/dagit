// cat-file implementation
#include "repo.h"

int cat_file(int argc, char* argv[]){
    std::string currentDir = std::filesystem::current_path();
    Repo* repo = repo->findRepo();
    
    return 0;
}